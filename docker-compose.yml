services:
  app:
    build: .
    expose:
      - "8000"
    # Published only on localhost so an external (host) nginx can proxy to it.
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  nginx:
    # Bundled reverse proxy + static server. Only needed when you don't have
    # your own nginx: `docker compose --profile web up --build`.
    build:
      context: .
      dockerfile: Dockerfile.nginx
    profiles: ["web"]
    ports:
      - "80:80"
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped
